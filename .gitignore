# ---- Dependencies ----
node_modules/

# ---- Build output ----
dist/
dist-ssr/
# Rust / Tauri
src-tauri/target/
src-tauri/binaries/
# Tauri-generated schemas/capabilities (regenerated on build)
src-tauri/gen/

# ---- Release artifacts (built locally or in CI; never committed) ----
release/**/*.AppImage
release/**/*.deb
release/**/*.rpm
release/**/*.exe
release/**/*.msi
release/**/*.dmg
release/**/*.app
release/**/*.snap
release/**/*.tar.gz
release/**/*.sig
release/_artifacts/

# ---- Logs ----
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Local / env ----
*.local
.env
.env.*
!.env.example

# ---- Editor / OS ----
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vendored comparison clone (see TRADE_OFF.md)
/Pake/

# Store credentials exported locally (never commit)
DONT.md
# Using bun; npm lockfile is not used
package-lock.json
